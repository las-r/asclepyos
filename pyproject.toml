[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "asclepyos"
version = "v2026.1.0"
description = "A fantasy operating system shell written in Python."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
asclepyos = "asclepyos.__main__:main"
